# Copy to .env and adjust
SERVER=dev
MYSQL_PORT=3306
# MySQL root password - matches API default
MYSQL_ROOT_PASSWORD=root
# Optional app user
MYSQL_APP_USER=evs
MYSQL_APP_PASSWORD=change-me

# Optional: Redis Cache (v0.6.0+)
# Uncomment REDIS_PORT if you enable Redis in docker-compose.yml
# REDIS_PORT=6379
